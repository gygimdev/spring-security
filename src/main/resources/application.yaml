spring:
  security:
    user:
      name: testuser
      password: Test1234!

  thymeleaf:
    # 운영시 주의
    cache: false

  jpa:
    hibernate:
      # 운영시 주의
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  datasource:
    url: jdbc:mariadb://localhost:3306/spring_security
    username: root
    password: Test1234!